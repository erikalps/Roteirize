# URL de conexão com o Postgres (porta 5433 no host)
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/roteirize

# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=