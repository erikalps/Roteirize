# Roteirize — Web

Front-end do Roteirize, construído com React + TypeScript + Vite.

## Como rodar

Configurar variáveis de ambiente:

```bash
cp .env.example .env
```

Instalar dependências e rodar:

```bash
npm install
npm run dev
```

Acesse em `http://localhost:5173`.